# Kernel command line for the POCO X3 Pro.
#
# A safety net rather than a tuning: on this device the modem investigation
# regularly wedges the machine, and without these a hang means holding the
# button. Any hung task panics, and a panic reboots on its own.
hung_task_panic=1
hung_task_timeout_secs=30
panic=15
