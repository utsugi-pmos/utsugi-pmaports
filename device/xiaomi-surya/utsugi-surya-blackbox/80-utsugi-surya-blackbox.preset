enable caja-negra-ajustes.service
enable caja-negra-archivar.service
enable caja-negra-latido.timer
# sincronizar-journal is deliberately NOT enabled: it writes the journal to disk
# on every message, which costs flash writes. Turn it on only while chasing a
# hang that eats the last seconds of the log.
