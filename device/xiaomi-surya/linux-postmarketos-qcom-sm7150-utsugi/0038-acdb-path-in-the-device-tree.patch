Tell the kernel where the audio calibration database is

Goes with 0037: that one adds the mechanism to register topologies in the DSP,
and this one says which file to take them from.

  - The q6core node of sm7150.dtsi gets a label, which it did not have.
  - The path of the ACDB is given in surya's device tree.

The path is relative to the firmware tree (/lib/firmware). Surya's own ACDB
ALREADY comes in the firmware package, although hidden away:

    /usr/share/qcom/sm7150/Xiaomi/surya/acdb/QRD/QRD_Global_cal.acdb

(QRD is the commercial device variant; IDP is the development board and it
carries a different codec, tavil/WCD9340, which is not ours). It has to be
copied to the place this property names:

    cp /usr/share/qcom/sm7150/Xiaomi/surya/acdb/QRD/QRD_Global_cal.acdb \
       /lib/firmware/qcom/sm7150/xiaomi/surya/Global_cal.acdb

It brings 30 topologies, and from the voice range (0x10010Fxx) exactly ONE:

    0x10010f8b  with 15 modules   <- the device's transmit chain

That it is the only one and that it has a long chain (echo cancellation, noise
suppression) fits with it being the TX topology to use.

--- a/arch/arm64/boot/dts/qcom/sm7150.dtsi	2026-08-10 23:53:16.227458691 +0200
+++ b/arch/arm64/boot/dts/qcom/sm7150.dtsi	2026-08-10 23:53:16.229668929 +0200
@@ -2567,7 +2567,7 @@
 					#address-cells = <1>;
 					#size-cells = <0>;
 
-					service@3 {
+					q6core: service@3 {
 						compatible = "qcom,q6core";
 						reg = <APR_SVC_ADSP_CORE>;
 
diff -ruN a/arch/arm64/boot/dts/qcom/sm7150-xiaomi-surya.dtsi b/arch/arm64/boot/dts/qcom/sm7150-xiaomi-surya.dtsi
--- a/arch/arm64/boot/dts/qcom/sm7150-xiaomi-surya.dtsi	2026-08-10 23:53:16.692356605 +0200
+++ b/arch/arm64/boot/dts/qcom/sm7150-xiaomi-surya.dtsi	2026-08-10 23:53:21.540621886 +0200
@@ -464,3 +464,16 @@
 &wifi {
 	qcom,ath10k-calibration-variant = "xiaomi_surya";
 };
+
+/*
+ * Audio calibration database. The DSP does not accept a voice topology that has not
+ * been registered with it beforehand, and the topologies come from here. Without this the
+ * voice session is created without error and the transmit path comes out mute.
+ *
+ * For now the Pixel 4a (sunfish) one is used, which carries the same SoC: it is the
+ * only ACDB the firmware package ships. The right thing would be to extract the
+ * surya one from Xiaomi's vendor partition.
+ */
+&q6core {
+	qcom,acdb-name = "qcom/sm7150/xiaomi/surya/Global_cal.acdb";
+};
