# Card 0, PCM 4: the VoiceMMode1 DAI published by q6voice-dai.
# Verified in /proc/asound/pcm:  00-05: VoiceMMode1 (*)
# NOTE: changes if links are added or removed in the device tree.
q6voice_card=0
q6voice_device=5
