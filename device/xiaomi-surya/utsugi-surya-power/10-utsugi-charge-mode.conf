[Unit]
# In charge mode there is no graphical session; the condition is re-evaluated on
# every boot, so deleting /run/charge-mode brings Plasma back with no other change.
ConditionPathExists=!/run/charge-mode
