[Service]
# The phone gives applications 415 logical pixels of width. The portal's old
# QtWidgets dialogs are built assuming 500-600 and spill off both edges, with
# Cancel off-screen. More logical width for THIS process only makes them fit and
# wrap on their own, without shrinking the rest of the phone.
Environment=QT_SCALE_FACTOR=0.75
