[Service]
# The drawer overrides and the favourites bar, written before the shell reads
# them. The leading "-" makes a failure harmless: the shell starts regardless.
ExecStartPre=-/usr/libexec/utsugi-surya/shell-defaults
