maintainer="Dr. Utsugi <cidwel@gmail.com>"
pkgname=lost-phone
pkgver=1.6
pkgrel=0
pkgdesc="Find your phone without Google: SMS, your own network, your own relay or ntfy"
url="https://github.com/utsugi-pmos/lost-phone"
arch="all"
license="GPL-2.0-or-later"
options="!check"
subpackages="$pkgname-systemd"
depends="
	plasma-settings
	kcmutils
	kirigami
	qt6-qtdeclarative
	qt6-qtsvg
	modemmanager
	networkmanager-cli
	pulseaudio-utils
	alsa-utils
	plasma-mobile-sounds
	"
makedepends="
	cmake
	samurai
	extra-cmake-modules
	qt6-qtbase-dev
	qt6-qtdeclarative-dev
	kcmutils-dev
	kconfig-dev
	kcoreaddons-dev
	ki18n-dev
	kirigami-dev
	"
source="$pkgname-$pkgver.tar.gz::$url/archive/refs/tags/v$pkgver.tar.gz
	80-lost-phone.preset
	"

build() {
	cmake -B build -G Ninja \
		-DCMAKE_BUILD_TYPE=RelWithDebInfo \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DCMAKE_INSTALL_LIBDIR=lib
	cmake --build build
}

package() {
	DESTDIR="$pkgdir" cmake --install build
	# Not in the app drawer: it is configured from Settings (its KCM), so the
	# launcher tile is clutter. Hidden here (idempotently) until the source
	# .desktop ships NoDisplay itself.
	sed -i '/^NoDisplay=/d; /^\[Desktop Entry\]/a NoDisplay=true' \
		"$pkgdir"/usr/share/applications/org.kde.lostphone.desktop
	install -Dm644 "$srcdir"/80-lost-phone.preset -t "$pkgdir"/usr/lib/systemd/user-preset/
	# Opens the LAN door on wifi only; the daemon refuses to listen without it.
	install -Dm644 50_lost-phone.nft -t "$pkgdir"/etc/nftables.d/
}

systemd() {
	default_systemd
}
sha512sums="
d79811f088769080528afca64c8a7b7a84127e9579d785293475f22115155e4e6434669e85c9100233e6b47431b4a8cac19dbfb8d3b1cb6ed1a659fb6cf413eb  lost-phone-1.6.tar.gz
b0f9eb656292bc109414ee370aad7189691bba7160c676229cfe9f007957920ac9fb3413a9e2e86d8c2819037862bc1fd5a022de7e33d3955c91972b5a693229  80-lost-phone.preset
"
